<!--
  @file: diary.md
  @description: Дневник наблюдений проекта MemoMed AI -- решения, проблемы, контекст
  @dependencies: project.md, tasktracker.md
  @created: 2026-02-22
-->

# MemoMed AI -- Дневник разработки

> Этот документ обеспечивает бесшовную работу разработчиков.
> Каждая запись содержит контекст принятых решений, выявленных проблем и их решений.

---

## [2026-02-22] Аудит дизайна MedTech и правило для AI

### Наблюдения

Пользователь запросил подключение «скиллов дизайнера» и унификацию всех страниц под MedTech-стиль с эталоном dashboard. В skills-cursor нет отдельного designer skill — создано правило в `.cursor/rules/memomed-medtech-design.mdc`.

### Решения

1. **Правило memomed-medtech-design.mdc** — применяется к `**/*.tsx` и `**/*.css`. Содержит: эталон dashboard, обязательные классы (med-page, med-card, med-btn-primary и т.д.), цвета из globals.css, принципы адаптивности mobile-first, запреты (text-xs, min-h &lt; 48px, хардкод hex).

2. **Документ design-audit-medtech-2026-02-22.md** — полный аудит всех страниц (patient, relative, doctor, admin, auth). Для каждой страницы: текущее состояние, проблемы, приоритет. План доработок в 3 фазы: критичные (Feed, Admin), средние (Medications, Connect, Journal, Stats, Doctor), низкие (Auth, Settings и др.). Чеклист адаптивности.

### Следующие шаги

Реализация доработок по плану в design-audit-medtech-2026-02-22.md — **выполнено 2026-02-22**:

- Фаза 1: Feed (med-card, med-btn-primary, med-badge-info), Admin (bg-[#F0F4F8], med-stat, med-card, градиентные quick-links)
- Фаза 2: Medications, Connect, Journal, Stats, Doctor dashboard
- Фаза 3: Auth (bg), History, Invite

---

## [2026-02-22] Архитектурное проектирование: выбор стека и формирование документации

### Наблюдения

1. **Исходные документы (DOC/) описывают Flutter + Node.js/Express стек**, ориентированный на мобильное приложение. Однако заказчик уточнил, что нужен в первую очередь веб-портал с адаптивным дизайном и возможностью последующей упаковки в мобильные приложения. Это меняет приоритеты.

2. **Заказчик -- начинающий разработчик**. Это значит: стек должен быть единым (один язык), инструменты отладки -- максимально удобными, а архитектура -- не переусложнённой.

3. **DeepSeek выбран как LLM-провайдер** вместо OpenAI. Причина: удобная оплата из РФ. DeepSeek API полностью совместим с OpenAI SDK, поэтому переключение тривиально (замена `baseURL`). Стоимость в ~18 раз ниже GPT-4o.

4. **Бизнес-модель** -- бесплатный сервис для пользователей, монетизация через анонимизированную обратную связь о лекарствах для фармацевтических компаний. Это требует дополнительной таблицы `medication_feedback` и механизма анонимизации.

5. **Обнаружен баг в исходной SQL-схеме** (`DOC/med_assistant_db_schema.sql`, строки 189-197): функция `reset_daily_medication_flags()` сбрасывает `status = 'taken'` на `'pending'` для записей за прошлые дни. Это уничтожает историю приёмов. Логи должны быть иммутабельными.

### Решения

**Решение 1: Next.js вместо Flutter**

Рассмотренные варианты:

| Вариант                   | Плюсы                                                             | Минусы                                                |
| ------------------------- | ----------------------------------------------------------------- | ----------------------------------------------------- |
| Flutter Web + Mobile      | Один код для web и mobile                                         | Тяжёлый web-бандл, плохой SEO, Dart -- отдельный язык |
| Next.js + Capacitor       | Лучший web-first опыт, TypeScript везде, PWA, огромная экосистема | Capacitor добавляет прослойку для mobile              |
| Nuxt.js (Vue) + Capacitor | Проще порог входа, чем React                                      | Меньше экосистема, меньше русскоязычных ресурсов      |

**Выбран Next.js** потому что:

- Единый язык TypeScript для фронта и бэка (снижает когнитивную нагрузку)
- Самая большая экосистема и количество обучающих материалов на русском
- PWA из коробки -- offline-first без отдельного мобильного приложения
- Capacitor позволит упаковать в сторы позже
- shadcn/ui предоставляет доступные компоненты (критично для пожилых пользователей)

**Решение 2: Объединённый стек вместо разделённого**

Вместо отдельного Express-бэкенда используем Next.js API Routes для основной логики. Это сокращает количество проектов с двух до одного, упрощает деплой и разработку. Отдельный Worker-сервис оставляем только для фоновых задач (cron, очереди).

**Решение 3: SSE вместо WebSocket для живой ленты**

Server-Sent Events проще в реализации, чем WebSocket, и достаточны для однонаправленного потока данных (сервер → клиент). WebSocket был бы оверинжинирингом для этого случая.

**Решение 4: Prisma ORM вместо raw SQL**

Обеспечивает типобезопасность запросов, автоматические миграции и удобную работу со схемой. Для начинающего разработчика Prisma значительно понятнее, чем raw SQL-запросы.

**Решение 5: Invite-код увеличен до 12 символов**

Исходный 8-символьный код уязвим к перебору. 12 символов + rate limiting (5 попыток в час) делают brute-force непрактичным.

### Проблемы

1. **Offline AI-чат невозможен** -- DeepSeek API требует интернет. В offline-режиме чат будет показывать сообщение "AI-помощник доступен при подключении к интернету" и предлагать кэшированные ответы на частые вопросы.

2. **Web Push не поддерживается в iOS Safari** в полной мере (ограничения Apple). Для iOS-пользователей до упаковки через Capacitor push-напоминания будут работать только при открытом браузере. Это приемлемо для MVP, решится в v2.0 (Capacitor + native push).

3. **Партиционирование medication_logs** -- PostgreSQL партиционирование добавляет сложность. Для MVP откладываем, но закладываем структуру (timestamps на всех записях). Реализуем при достижении >100K записей.

---

## [2026-02-22] Результаты Q&A: все архитектурные вопросы решены

### Наблюдения

1. **Все 15 вопросов из qa.md получили ответы** от заказчика. Большинство рекомендаций приняты без изменений. Отклонений от рекомендаций -- два:
   - Q3 (TTS): рекомендовалось реализовать в MVP, решено отложить на v1.1
   - Q11 (обратная связь): добавлено уточнение -- freemium-модель (минимум бесплатно, глубокая аналитика по подписке)

2. **Бизнес-модель уточнена**: не просто продажа данных, а freemium-аналитика. Бесплатный уровень привлекает фарм-компании, подписка монетизирует глубокие инсайты. Это влияет на архитектуру API обратной связи (нужны разные уровни доступа к агрегированным данным).

3. **Добавлены 2 новые таблицы в схему БД**:
   - `chat_messages` -- серверное хранение истории AI-чата (ранее передавалась с клиента)
   - `ai_usage_stats` -- мониторинг расхода токенов DeepSeek

4. **Token budget определён**: 50 000 токенов/месяц на пользователя. Хранение в Redis (быстрый доступ), агрегация в PostgreSQL (аналитика).

### Решения

**Решение 6: Серверное хранение истории чата вместо клиентского**

Исходный код в DOC/ передавал `conversationHistory` с клиента. Это ненадёжно (теряется при перезагрузке), небезопасно (клиент может подменить историю) и не позволяет анализировать вопросы. Теперь история хранится в таблице `chat_messages` с автоочисткой через 30 дней.

**Решение 7: Freemium-аналитика вместо прямой продажи данных**

Два уровня доступа к агрегированной обратной связи:

- Бесплатный: базовые метрики (средняя оценка, top побочных эффектов)
- Подписка: глубокая сегментация, тренды, корреляции

Это влияет на API: нужен middleware для уровней доступа к `/api/feedback/aggregate`.

**Решение 8: Согласие на данные в MVP**

Форма согласия на обработку персональных данных (ФЗ-152) реализуется сразу в MVP, а не откладывается. Отдельный чекбокс для программы обратной связи. Это правильно с юридической точки зрения и не требует значительных усилий.

### Проблемы

Нет новых проблем. Все открытые вопросы закрыты.

---

---

## [2026-02-22] Этап 2: Фото, Дневник, Отзывы, PDF-отчёт, TTS

### Завершённые задачи

**1. Загрузка фото лекарств**

- `src/lib/storage/storage.service.ts` — абстракция хранилища (local/S3). Динамический импорт AWS SDK с `/* @vite-ignore */` и алиасом в vitest.config.ts
- `src/app/api/medications/[id]/photo/route.ts` — POST/DELETE эндпоинт
- `src/components/patient/photo-upload.tsx` — клиентский компонент с Canvas API (сжатие до 500px WebP, drag-and-drop, камера)
- 5 тестов в `tests/unit/photo-upload.test.ts`

**2. Дневник самочувствия**

- `src/lib/validations/journal.schema.ts` — Zod-схема
- `src/app/api/journal/route.ts` и `sync/route.ts` — CRUD + batch sync
- `src/components/patient/journal-form.tsx` — форма с 4 ползунками (1-5) + свободный текст
- Страницы `/journal` и `/journal/[date]`
- IndexedDB обновлён до версии 2 (таблица `journal`)
- 9 тестов Zod-схемы
- Дневник добавлен в нижнюю навигацию (BottomNav)

**3. Обратная связь по лекарствам**

- `src/lib/ai/feedback-analyzer.ts` — AI-анализ через DeepSeek (побочки, concernLevel, recommendation)
- `src/app/api/feedback/route.ts` — POST (отзыв+AI) и GET (история)
- `src/app/api/feedback/aggregate/route.ts` — анонимная статистика для admin
- `src/components/patient/feedback-form.tsx` и `feedback-modal.tsx`
- Интегрировано в страницу редактирования лекарства

**4. PDF-отчёт для врача**

- Установлен `@react-pdf/renderer@4.3.2`
- `src/lib/ai/report.prompt.ts` — AI-генерация текстового резюме
- `src/components/shared/pdf-report.tsx` — React-PDF документ с кириллицей (Roboto)
- `src/app/api/reports/[patientId]/route.ts` — GET эндпоинт, доступен пациенту и его родственникам
- `src/components/shared/download-report-button.tsx` — кнопка с прогрессом
- Кнопки 30d/90d/180d добавлены на `/stats` страницу

**5. Text-to-Speech**

- `src/hooks/use-tts.ts` — хук Web Speech API (настройки в localStorage)
- `src/components/shared/tts-settings.tsx` — компонент управления голосом
- `src/app/(patient)/settings/page.tsx` — страница настроек (Push + TTS + профиль)
- `TakeMedicationButton` расширен: prop `autoAnnounce`, `dosage`, `scheduledTime`

### Решения

**Решение 1: S3 как опциональная зависимость**
AWS SDK импортируется только при `STORAGE_TYPE=s3` через динамический импорт. Для тестов добавлен алиас в `vitest.config.ts` → `tests/mocks/aws-sdk-s3.ts`.

**Решение 2: PDF + кириллица**
React-PDF требует явного подключения шрифтов с кириллицей. Выбран Roboto через CDN.

**Решение 3: Статистика обратной связи**
`/api/feedback/aggregate` доступен только role=admin и только при ≥5 отзывов (анонимность K-анонимности).

### Статистика тестов

- Всего тестов: 57 (8 файлов)
- Все прошли успешно ✅

---

## [2026-02-22] Аудит кода, версия сборки, скрипты запуска

### Версия сборки

- `next.config.ts`: добавлены `NEXT_PUBLIC_BUILD_VERSION`, `NEXT_PUBLIC_BUILD_DATE`, `NEXT_PUBLIC_BUILD_COMMIT`
- `src/components/shared/build-info.tsx` — компонент отображения: `compact` (только v0.1.0) и `full` (версия + дата + коммит)
- Встроен в `/settings` и `/api/health`

### Аудит кода — найдено и исправлено

**Критичные:**

1. Отсутствующие индексы Prisma: `Connection(relativeId, status)`, `MedicationLog(medicationId, createdAt)` → добавлены
2. `prisma.ts` не проверял наличие `DATABASE_URL` → добавлена проверка с понятным сообщением об ошибке

**Безопасность:** 3. Rate limiting на `/api/reports` (5/час) и `/api/feedback` (10/час) → добавлен 4. Middleware: улучшен matcher (исключены /uploads/, sw.js, workbox-\*), `callbackUrl` при редиректе на /login, `Set` вместо массива для PUBLIC_ROUTES

**Качество кода:** 5. `src/types/index.ts` — удалены неиспользуемые типы (`SessionUser`, `MedicationWithLogs`), `FeedEvent` оставлен только в одном месте 6. `src/lib/env.ts` — валидация ENV-переменных через Zod при старте 7. `.env.example` — добавлены `NEXT_PUBLIC_VAPID_PUBLIC_KEY` и `NEXT_PUBLIC_UPLOAD_MAX_SIZE_MB`

### Скрипты

1. **`scripts/setup.sh`** — первоначальная настройка:
   - Проверка Node.js >= 20, Docker (при --docker)
   - Создание .env из .env.example с автогенерацией секретов (NEXTAUTH_SECRET, POSTGRES_PASSWORD, VAPID)
   - npm install, prisma generate, prisma migrate
   - Git init + Husky

2. **`scripts/start.sh`** — запуск:
   - `./scripts/start.sh` → dev (npm run dev)
   - `./scripts/start.sh --prod` → build + start
   - `./scripts/start.sh --docker` → docker compose up -d с healthcheck

3. В `package.json` добавлены: `setup`, `setup:docker`, `start:prod`, `start:docker`

### Статистика тестов после аудита

- 57 тестов, 8 файлов — все пройдены ✅

---

## [2026-02-22] Автономия проекта, Статистика и дашборд

### Автономия проекта

- Проект отвязан от контейнеров другого проекта (infra_db_1, infra_redis_1)
- `docker-compose.dev.yml`: собственные контейнеры `memomed_postgres` (порт 5433) и `memomed_redis` (порт 6380)
- `docker-compose.yml` (production): согласован с `MEMOMED_PG_PASSWORD`
- Скрипты `setup.sh` и `start.sh` обновлены: автозапуск контейнеров, поддержка podman-compose
- `prisma.config.ts`: ручная загрузка .env (Prisma 7 не делает это автоматически)
- Next.js 16: `serverComponentsExternalPackages` → `serverExternalPackages`, добавлен `turbopack: {}`
- Middleware: разделён auth.config.ts на edge-safe (для middleware) и full (с bcrypt/Prisma)
- Конфликт маршрутов `/chat`: убран дубль, чат перенесён в `(shared)` route group

### Статистика и дашборд (Этап 2)

- `src/lib/stats/stats.service.ts` — чистые функции расчёта метрик:
  - `calcAvgDelayMinutes`: средняя задержка приёма (по taken с actualAt)
  - `calcStreak`: текущая серия + рекорд (100% дни, пропускает дни без приёмов)
  - `buildDailyTrend`: группировка логов по дням, процент за каждый день
  - `calculateStats`: полная агрегация (процент, streak, задержка, тренд)
- `src/app/api/stats/[patientId]/route.ts` — GET с параметром `?days=N` (1-365)
  - Доступ: сам пациент или привязанный родственник
- `src/components/shared/stats-cards.tsx` — 4 карточки: дисциплина, streak, задержка, приёмы
  - Цветовая индикация: зелёный (≥90%), жёлтый (≥70%), красный (<70%)
- `src/components/shared/discipline-chart.tsx` — SVG-столбчатая диаграмма (без внешних зависимостей)
  - Тултипы при наведении, подписи дат, цветовая легенда
- `src/components/shared/stats-dashboard.tsx` — обёртка с переключателем периода (7д/2нед/месяц/3мес)
- Страница `/stats` обновлена: дашборд → календарь → отчёты

### Онбординг (Этап 2)

- `src/components/shared/onboarding-wizard.tsx` — 4 полноэкранных шага (эмодзи + крупный текст)
  - Шаги: Приветствие → Лекарства → Кнопка приёма → Родственники
  - Кнопка «Пропустить», навигация индикаторами, анимация
- `src/app/(shared)/onboarding/page.tsx` — server component, проверка onboardingDone
- `src/app/(shared)/onboarding/onboarding-client.tsx` — client обёртка, POST /api/profile/onboarding
- `src/app/api/profile/onboarding/route.ts` — upsert Profile.onboardingDone=true
- Dashboard: редирект на /onboarding если onboardingDone=false

### AI-анализ корреляций (Этап 3)

- `src/lib/ai/prompts/analyze.prompt.ts` — системный промпт с temperature 0.1:
  - Вход: лекарства, дисциплина, дневник самочувствия, даты пропусков
  - Выход: JSON (patterns, recommendations, overallAssessment, riskLevel)
- `src/app/api/analysis/[patientId]/route.ts`:
  - Собирает данные за 30 дней (лекарства + логи + дневник)
  - Кэш Redis 24ч, rate limit 3/час
  - Graceful fallback при ошибке AI
- `src/components/shared/analysis-results.tsx`:
  - Кнопка «Запустить анализ» (анализ по запросу, не автоматически)
  - Паттерны с иконками и уровнем уверенности
  - Рекомендации, riskLevel бейдж, медицинский дисклеймер
- Встроен в страницу /stats между дашбордом и календарём

### Решения

- AI-анализ по запросу, а не по cron — экономит токены DeepSeek, результат кэшируется на 24ч
- Метеоданные не блокируют анализ — работает на данных дневника + логов

### Статистика тестов

- 71 тест, 9 файлов — все пройдены ✅
- Новые: 14 тестов stats.service (calcAvgDelay, calcStreak, buildDailyTrend, calculateStats)

---

## Сессия 4 (2026-02-22) — Роль Врача, Метеоданные, Админ-панель

### Роль "Врач" (Этап 5)

- **Миграция**: `doctor` добавлен в Role enum (`20260222085721_add_doctor_role_and_meteo`)
- **TypeScript**: `next-auth.d.ts`, `auth.config.ts`, `actions.ts` обновлены для нового типа
- **Регистрация**: форма теперь предлагает 3 роли (пациент / родственник / врач)
- **Навигация**: редирект на `/doctor/dashboard` после входа/регистрации
- **API**: `GET /api/doctor/patients` — список подключённых пациентов + дисциплина 30 дн.
- **Pages**:
  - `(doctor)/doctor/dashboard` — список пациентов с % дисциплины, цветная индикация
  - `(doctor)/doctor/patients/[patientId]` — детальная карточка: лекарства, статистика, AI-анализ, отчёты
- **Layout**: `(doctor)/layout.tsx` — role-guard + Header + BottomNav
- Врач подключается к пациентам через существующий invite-код механизм (Connection)

### Интеграция метеоданных (Этап 3)

- **Prisma**: модель `MeteoLog` (temperature, humidity, pressure, weatherMain, regionCode, logDate)
- **Worker**: `fetch-weather.job.ts` — BullMQ worker, cron каждые 3 часа, опциональный (пропускается без `OPENWEATHER_API_KEY`)
- **AI-промпт**: `analyze.prompt.ts` расширен — если есть метеоданные, AI анализирует корреляции погода → самочувствие
- **API анализа**: `GET /api/analysis/[patientId]` теперь подгружает MeteoLog за 30 дн. и передаёт в промпт
- **Env**: `.env.example` дополнен `OPENWEATHER_API_KEY` с инструкцией получения

### Админ-панель (Этап 5)

- **Layout**: `(admin)/layout.tsx` — боковое меню, role-guard (admin only)
- **Pages**:
  - `/admin` — обзор платформы: 5 ключевых метрик, распределение ролей, последние 5 регистраций
  - `/admin/users` — список пользователей с пагинацией (20/стр.), фильтр по роли
  - `/admin/prompts` — список prompt_templates с inline-редактором (personaBlock/contextBlock/taskBlock)
  - `/admin/stats` — статистика AI: токены/запросы всего и за месяц, топ-10 пользователей по токенам
- **Компоненты**: `PromptEditor` — expandable секции, редактирование в textarea, сохранение через Server Action
- **Server Actions**: `updatePromptAction`, `deletePromptAction` в `lib/admin/prompt.actions.ts`

### Решения

- Роль doctor использует существующую Connection модель — нет лишних таблиц
- Worker для метеоданных полностью опциональный — приложение работает без него
- Админ-панель доступна только пользователям с role=admin (проверка на уровне layout)

### Статистика тестов

- 71 тест, 9 файлов — все пройдены ✅ (без регрессий)

---

## Сессия 5 (2026-02-22) — Отзывы о лекарствах, Экспорт отчётов, Локализация

### Сбор обратной связи (Этап 3) — UI

- **Страница /feedback**: список активных лекарств + форма отзыва + история последних 5 отзывов
- **FeedbackForm**: выбор лекарства, оценка звёздочками (1-5), поле побочных эффектов, комментарий
- **Навигация**: добавлена кнопка "Отзывы 📝" в BottomNav пациента
- **Тесты**: 11 тестов для `medicationFeedbackSchema` (валидация UUID, диапазоны, длины строк)

### Экспорт отчётов для врача (Этап 3) — подтверждено

- `src/lib/ai/report.prompt.ts` — уже реализован ранее (AI-резюме через DeepSeek)
- `src/components/shared/pdf-report.tsx` — уже реализован ранее (@react-pdf/renderer + Roboto кириллица)
- `src/app/api/reports/[patientId]/route.ts` — уже реализован ранее (period param, rate limit)
- Статус задачи исправлен в tasktracker.md

### Локализация (Этап 5)

- **next-intl v4**: установлен, плагин в next.config.ts
- **Конфиг**: `src/i18n/request.ts` — locale из cookie `NEXT_LOCALE` (по умолчанию `ru`)
- **Переводы**: `messages/ru.json` и `messages/en.json` — 8 разделов, ~120 ключей:
  - common, auth, nav, dashboard, medications, stats, journal, feedback, chat, feed, doctor, settings
- **Provider**: `NextIntlClientProvider` добавлен в root layout (server component, передаёт messages)
- **LanguageSwitcher**: кнопка в Header — переключает RU↔EN, записывает cookie, перезагружает страницу
- **API**: `POST /api/locale` — устанавливает cookie `NEXT_LOCALE` на 1 год
- **Интеграция**: `BottomNav` и `FeedbackForm` переведены на `useTranslations`

### Решения

- Локализация без изменения URL-структуры (нет `/en/...`) — минимальный рефакторинг
- Переключение через cookie + reload — надёжно, без client-side routing complications

### Статистика тестов

- **82 теста, 10 файлов** — все пройдены ✅ (+11 тестов feedback.schema)

---

## Сессия 6 (2026-02-22) — Качество кода, Capacitor Android, Дополнительные тесты

### Исправление pre-existing TypeScript ошибок (0 ошибок)

- **`use-push-notifications.ts`**: исправлен возвращаемый тип `urlBase64ToUint8Array` → `Uint8Array<ArrayBuffer>`, переписан цикл без `Uint8Array.from`
- **`reminder.job.ts`**: заменён `redis` (ioredis-инстанс) на `redisConnection` (config object) для BullMQ, исправлена типизация имени задачи (`as string`)
- Результат: **`npx tsc --noEmit --skipLibCheck` — 0 ошибок**

### Capacitor для Android (Этап 4)

- **`capacitor.config.ts`**: appId `ai.memomed.app`, плагины Firebase Push + Camera + SplashScreen
- **`next.config.ts`**: поддержка `BUILD_TARGET=capacitor` → `output: 'export'` + `trailingSlash: true`
- **`src/hooks/use-capacitor.ts`**: хук `useCapacitorEnv` (isNative/isAndroid/isIos), функции `capturePhotoNative` и `registerNativePush` — безопасная деградация в браузере
- **`scripts/build-android.sh`**: автоматизированная сборка debug APK и release AAB
- **`package.json`**: скрипты `build:android`, `android:debug`, `android:release`, `android:open`
- **`docs/mobile.md`**: полная инструкция по сборке, настройке Firebase, отладке на устройстве

### Дополнительные тесты

- **`tests/unit/locale.test.ts`**: 8 тестов — isValidLocale, SUPPORTED_LOCALES
- **`tests/unit/analyze.prompt.test.ts`**: 12 тестов — buildAnalysisPrompt (содержимое промпта, метеоданные, дисклеймер)

### Итоги

- **102 теста, 12 файлов** — все пройдены ✅ (+20 новых тестов)
- **0 TypeScript ошибок** (исправлены все pre-existing проблемы)

---

## Сессия 7 (2026-02-22) — UX-улучшения, настройки врача, расширение тестов

### Исправление маршрутизации ролей

- **`(patient)/layout.tsx`**: добавлены редиректы для `doctor` → `/doctor/dashboard` и `admin` → `/admin`, предотвращает доступ к пациентской UI для других ролей

### Страница настроек пациента

- **`(patient)/settings/page.tsx`**: добавлена секция «🌐 Язык интерфейса» с компонентом `LanguageSwitcher`, исправлено отображение роли через словарь `ROLE_LABELS` (покрывает patient, relative, doctor, admin)

### Страница настроек врача

- **`(doctor)/doctor/settings/page.tsx`**: создана страница настроек для роли `doctor` — профиль, язык интерфейса, конфиденциальность, кнопка выхода
- **`bottom-nav.tsx`**: в `doctorNavItems` добавлен пункт «⚙️ Настройки» (ссылка `/doctor/settings`)
- **`messages/ru.json`, `messages/en.json`**: добавлен ключ `nav.settings`

### MSW-обработчики

- **`tests/mocks/handlers.ts`**: добавлены обработчики для `POST /api/logs/sync`, `POST /api/feedback`, `POST /api/locale`, `GET /api/medications`, `GET /api/analysis/:patientId`, `GET /api/stats/:patientId` — устраняет `onUnhandledRequest: 'error'` при будущих компонентных тестах

### Дополнительные тесты

- **`tests/unit/chat.prompt.test.ts`**: 14 тестов — проверка CHAT_SYSTEM_PROMPT, CHAT_DISCLAIMER, AI_FIRST_USE_DISCLAIMER (наличие ключевых правил, дисклеймеров, соответствие содержания)

### Итоги

- **116 тестов, 13 файлов** — все пройдены ✅ (+14 новых тестов)
- **0 TypeScript ошибок**

---

## Сессия 8 (2026-02-22) — Полный аудит и редизайн UX/UI в стиле MedTech

### Дизайн-система

- **`globals.css`**: полная замена цветовой системы — медицинский синий `#1565C0` как основной (вместо фиолетового), прохладный фон `#F0F5FA`, профессиональные статусные цвета, CSS-утилиты `.med-card`, `.med-btn-primary`, `.med-input`, `.med-label`, `.med-badge-*`, скелетон-анимации

### Иконки

- **`nav-icons.tsx`** (новый файл): 23 SVG-иконки для навигации и интерфейса — HomeIcon, PillIcon, BookIcon, StarIcon, BotIcon, UsersIcon, SettingsIcon, LogOutIcon, MedCrossIcon, EditIcon, TrashIcon, DownloadIcon, PlusIcon, CheckIcon и др. Заменяют эмодзи во всём приложении

### Шапка (Header)

- **`header.tsx`**: медицинский логотип с крестом в синем прямоугольнике, бейдж роли на десктопе, имя пользователя с иконкой UserIcon, обновлённый SignOutButton и LanguageSwitcher

### Нижняя навигация

- **`bottom-nav.tsx`**: SVG-иконки вместо эмодзи, активный элемент — пилюльный индикатор с синим фоном, увеличение иконки при активации

### Аутентификация

- **`login/page.tsx`**: двухколоночная верстка (левая панель — синий градиент с брендингом и USP, правая — форма), мобильный вариант — одноколоночный
- **`register/page.tsx`**: исправлен баг `<link>` → `<Link>`, блок безопасности с иконкой замка, профессиональный дизайн

### Страницы пациента

- **`dashboard/page.tsx`**: белая карточка с закруглённым верхом на синем фоне, клинические быстрые ссылки в сетке 3×2 с SVG-иконками, карточка «все принято» в зелёном стиле
- **`medications/page.tsx`**: клинические карточки с левой синей полосой акцента, SVG-иконки действий
- **`feedback/page.tsx`**: обновлённый дизайн страницы с историей отзывов в клиническом стиле
- **`feedback-form.tsx`**: улучшенная форма — заголовок с иконкой, ARIA-метки на звёздах, индикатор загрузки в кнопке, стилизованные метки `med-label`

### Страницы врача и родственника

- **`doctor/dashboard/page.tsx`**: аватары пациентов с первой буквой имени, цветовые бейджи дисциплины (зелёный/янтарный/красный), легенда статусов
- **`(relative)/feed/page.tsx`**: live-индикатор обновления, улучшенное пустое состояние

### Компоненты

- **`sign-out-button.tsx`**: иконка LogOutIcon, красная hover-реакция
- **`language-switcher.tsx`**: иконка GlobeIcon, компактный вид

### Итоги Сессии 8

- **116 тестов, 13 файлов** — все пройдены ✅
- **0 TypeScript ошибок**
- **Изменено 13 файлов, создано 2 новых**

---

## Сессия 9 — MedTech Design System v2 (тренды 2025-2026)

**Дата**: 2026-02-22  
**Цель**: Полная переработка дизайна под тренды 2025-2026 — glassmorphism, bento grid, staggered animations, gradient icons, micro-interactions

### Дизайн-система (`globals.css`)

- **Glassmorphism карточки**: `.med-card` с `backdrop-blur(12px)`, полупрозрачный белый фон, hover с усилением тени
- **Bento Grid**: `.med-bento` (2 колонки мобайл → 3 колонки десктоп), `.med-bento-wide` для двойной ширины
- **Staggered Animations**: `.med-stagger` — каскадное появление дочерних элементов с задержкой 60ms
- **Gradient кнопки**: `.med-btn-primary` с `linear-gradient`, `translateY(-1px)` при hover, `scale(0.98)` при active
- **Ghost кнопки**: `.med-btn-ghost` для второстепенных действий
- **Stat карточки**: `.med-stat` с `backdrop-blur(8px)`, `translateY(-2px)` при hover
- **Float animation**: `.med-float` — лёгкое покачивание для пустых состояний
- **Scale-in**: `.med-scale-in` для модальных окон
- **Gradient border**: `.med-gradient-border` — CSS-маска для градиентной границы
- **Dot pattern**: `.med-dots` — технологичный паттерн точек
- **Smooth scrollbar**: `.med-scroll` — стилизованный скроллбар

### Замена цветовой палитры (31 файл)

- `#7e57c2` → `#1565C0` (Medical Blue) — во всех файлах
- `#6a3fb5/#6a3fbf` → `#0D47A1` (hover) — во всех файлах
- `#ede7f6` → `#E3F2FD` (light bg) — 10 файлов
- `#d1c4e9` → `#BBDEFB` — 5 файлов
- `#9575cd` → `#42A5F5` — 2 файла
- `purple-*` → `blue-*` (Tailwind классы) — 6 файлов

### Компоненты форм

- **`login-form.tsx`**: `.med-input`, uppercase labels, убраны inline фиолетовые стили
- **`register-form.tsx`**: group-hover на чекбоксах, зелёная кнопка регистрации
- **`submit-button.tsx`**: gradient shadow `shadow-blue-200/60`, `active:scale-[0.98]`
- **`form-error.tsx`**: SVG AlertTriangleIcon вместо эмодзи

### Дашборд пациента

- **`dynamic-background.tsx`**: синяя палитра градиентов, dot-pattern через CSS `backgroundImage`, 3 стеклянных круга с `blur`
- **`live-clock.tsx`**: `text-6xl font-black`, `drop-shadow`, `med-pulse` скелетон
- **`next-medication-card.tsx`**: glassmorphism (`backdrop-blur-md`), gradient pill icon, пульсирующий бейдж при urgency
- **`take-medication-button.tsx`**: `bg-gradient-to-r from-green to-emerald`, SVG иконки (CheckIcon, AlertTriangleIcon), офлайн-баннер с пульсирующей точкой
- **`dashboard/page.tsx`**: bento grid (`.med-bento`) для быстрых ссылок, gradient иконки в каждой ячейке, staggered animation, `rounded-[2rem]` для основной панели

### Страница лекарств

- **`medication-card.tsx`**: pill icon в `bg-blue-50`, time badge в `bg-blue-50 rounded-lg`
- **`delete-medication-button.tsx`**: hover → `bg-red-50 text-red-500`, SVG TrashIcon
- **`medications/page.tsx`**: `.med-card-accent` карточки, `.med-stagger`, gradient кнопка добавления, `.med-float` на пустом состоянии

### Настройки

- **`settings/page.tsx`**: gradient icon headers для каждой секции (Profile=blue, Language=teal, Push=amber, TTS=indigo, Privacy=slate, About=gray), `.med-card` glassmorphism, `.med-badge-info` для роли

### AI-чат

- **`ai-chat.tsx`**: glassmorphism header (`bg-white/80 backdrop-blur-md`), gradient bot icon, `.med-float` empty state, `.med-scroll` scrollbar, red alert banner для ошибок

### Онбординг

- **`onboarding-wizard.tsx`**: SVG-иконки вместо эмодзи (HeartPulseIcon, PillIcon, CheckIcon, UsersIcon), glassmorphism icon container, dot-pattern, градиентные фоны, белая кнопка на тёмном фоне

### Лента родственника

- **`feed-item.tsx`**: gradient status icons, backdrop-blur карточки, SVG CheckIcon/AlertTriangleIcon
- **`live-feed.tsx`**: gradient пустое состояние, `.med-stagger`, SVG ActivityIcon

### Прочие компоненты

- **`offline-indicator.tsx`**: SVG AlertTriangleIcon, amber gradient
- **`build-info.tsx`**: uppercase tracking labels, divide-y разделители

### Итоги Сессии 9

- **116 тестов, 13 файлов** — все пройдены ✅
- **0 TypeScript ошибок**
- **Обновлено ~40 файлов**
- **Ключевые тренды**: glassmorphism, bento grid, staggered animations, gradient icons, micro-interactions, dot-pattern backgrounds

---

## [2026-02-22] Тестовые данные, режим отладки и расширение панели администратора

### Задача

Добавить реалистичные тестовые данные для разработки, удобный режим отладки без паролей, и расширить интерфейс администратора.

### Решения

**1. Seed-файл (`prisma/seed.ts`)**

- 50 пациентов (русские имена и фамилии, возраст 60-85 лет)
- 5 врачей с разными специальностями (кардиолог, терапевт, невролог, эндокринолог, гастроэнтеролог)
- 5 родственников, каждый связан с 5 пациентами
- 1 администратор: `admin@memomed.dev`
- Каждый пациент: 5-10 лекарств из банка 25 реальных препаратов (метформин, амлодипин, аторвастатин и др.)
- Логи приёмов за 30 дней: ~75% взяты, ~15% пропущены, ~10% pending
- Записи дневника самочувствия за 14 дней с реалистичными оценками
- Все пароли: `Test1234!`
- Команда: `npm run db:seed`

**2. Режим отладки**

- Новая страница `/dev-login` (только в `NODE_ENV=development`)
- Показывает карточки для быстрого входа: Admin, Doctor1, Doctor2, Relative1-2, Patient1-2, Patient10
- Сетка всех 50 пациентов (кнопки #1–#50)
- Реализовано через Server Action `devLoginAction(email, role)` — использует тот же `signIn('credentials')` с захардкоженным паролем `Test1234!`
- `auth.config.ts` дополнен: в dev-режиме `/dev-login` добавляется в список публичных страниц
- На странице `/login` в dev-режиме показывается жёлтый баннер со ссылкой на `/dev-login`
- В шапке `/admin` в dev-режиме показывается кнопка "🔧 Dev Login"

**3. Расширение панели администратора**
Добавлены новые страницы и разделы:

- `/admin/medications` — таблица всех лекарств с фильтрами (активные/архив), поиском по названию, топ-10 препаратов
- `/admin/connections` — управление связями пациент-родственник с фильтром по статусу
- `/admin/audit` — аудит-логи с фильтрацией по действию/пользователю, статистика действий
- Обновлён `layout.tsx`: расширено боковое меню (7 пунктов вместо 4)
- Обновлена главная `/admin`: добавлен блок быстрых ссылок, карточка лекарств стала кликабельной

### Архитектурные заметки

- `devLoginAction` вынесен в отдельный файл `src/lib/auth/dev-actions.ts` чтобы не засорять основной `actions.ts`
- В `seed.ts` используется `upsert` для идемпотентного запуска (повторный `db:seed` не создаёт дублей)
- `bcrypt.hash` с rounds=10 (вместо 12 в production) для скорости при сидировании
- Связи пациент-родственник: первые 25 пациентов привязаны к 5 родственникам (по 5 пациентов на родственника)

### Итоги Сессии 10

- **9 файлов созданы/изменены**
- Тестовые данные: 50 пациентов × 7.5 лекарств = ~375 лекарств, ~11 250 логов приёмов
- Режим отладки: 0 паролей, мгновенный вход для любой роли

---

## [2026-02-22] UX-улучшения: редизайн страниц, пагинация, новые маршруты

### Задача

Переработать неинформативные страницы под единый MedTech стиль, добавить пагинацию, устранить пробелы в навигации для ролей родственник и врач.

### Решения

**1. Страница врача `/doctor/patients/[id]`**

- Геро-карточка: аватар-инициал, возраст, email, часовой пояс, дата подключения, % дисциплины (цветовой индикатор)
- 4 вкладки (URL `?tab=`): Обзор, Лекарства, Дневник, Отчёты
- Обзор: StatsDashboard + последние 10 приёмов с временем план/факт
- Лекарства: каждый препарат с прогресс-баром дисциплины за 30 дней (принято/пропущено/%)
- Дневник: записи самочувствия с пагинацией (14/стр.)
- Отчёты: AI-анализ + PDF за 30/90/180 дней + сводная таблица

**2. Страницы пациента**

- `/journal` — пагинация 10/стр., числовые шкалы /5, прогресс-бары, цветовые карточки
- `/stats` — обёртка `med-page`, карточки PDF-отчётов
- `/history` — пагинация 20/стр., фильтры по статусу (Все/Принято/Пропущено/Ожидает), группировка по датам
- `/invite` — редизайн, список подключённых с ролями и статусами, пошаговая инструкция

**3. Родственник**

- `/account` — новая страница настроек (профиль, язык, push, выход). Путь `/account` выбран для избежания конфликта с `/settings` пациента
- `/patients/[id]` — новая страница просмотра пациента: геро-карточка, вкладки Обзор/Лекарства/Дневник
- `/connect` — редизайн в `med-page` стиле, список подключённых с переходом в профиль
- `BottomNav`: добавлен пункт «Настройки» → `/account`
- `FeedItem`: карточка стала ссылкой на `/patients/[patientId]`

**4. Врач**

- `/doctor/connect` — новая страница ввода инвайт-кода, список уже подключённых пациентов
- `/doctor/dashboard` — пагинация 12/стр., фильтры по дисциплине (≥90%, 70–89%, <70%, нет данных), поиск по имени
- `BottomNav`: добавлен пункт «Подключить» → `/doctor/connect`

**5. Прочее**

- `nav-icons.tsx`: добавлен `SearchIcon`
- `messages/ru.json`, `messages/en.json`: ключ `connect` для навигации

### Исправление конфликта маршрутов

- Изначально `/settings` использовался и пациентом, и родственником — конфликт в Next.js App Router
- Решение: настройки родственника перенесены на `/account`, BottomNav обновлён

### Итоги Сессии 11

- **~15 файлов созданы/изменены**
- Единый UX для всех ролей: геро-карточки, вкладки, пагинация, цветовые индикаторы

---

## [2026-02-22] Unit-тесты для Web Push

### Задача

Добавить unit-тесты для сервиса push-уведомлений (задача Web Push напоминания в tasktracker).

### Решение

Создан файл `tests/unit/push.service.test.ts` с тестами для `buildMedicationReminderPayload`:

- T+0: заголовок «Время принять», текст «запланировано в HH:MM»
- T+10, T+20, T+30: заголовок «Не забыли про X?», текст «N минут назад»
- Проверка actions: `taken`, `snooze`
- Проверка tag, data.url, dosage в body

Функция `buildMedicationReminderPayload` — чистая, без зависимостей от БД/Redis, поэтому тестируется без моков.

**Дополнительно (2026-02-22):**

- Выделен модуль `src/lib/reminders/schedule-utils.ts` с чистыми функциями: `computeNextScheduledAt`, `getReminderJobId`, `getReminderJobIds`
- `queue.ts` переведён на использование schedule-utils
- Добавлены тесты `tests/unit/reminder-schedule.test.ts`: расчёт следующего времени приёма, формат jobId, список jobId для отмены

---

## [2026-02-22] Документация публикации в Google Play

### Задача

Подготовить всё необходимое для публикации Android-приложения в Google Play (кроме самого акта публикации, требующего устройство и Developer Account).

### Решение

Добавлен раздел «Публикация в Google Play» в `docs/mobile.md`:

- Предварительные шаги (регистрация, AAB)
- Чеклист Store Listing: описание, иконки, скриншоты, политика конфиденциальности, Data safety
- Рекомендуемые скриншоты (главный экран, лекарства, лента, AI-чат, статистика)
- Требования к политике конфиденциальности

---

## [2026-02-22] GitHub Actions CI

### Задача

Настроить CI (см. qa.md Q6): lint + type-check + tests на push.

### Решение

Создан `.github/workflows/ci.yml`:

- Триггер: push и pull_request на ветки main, develop
- Шаги: checkout → Node 20 → npm ci → prisma generate → lint → type-check → test
- Без БД/Redis: unit-тесты используют моки

---

## [2026-02-22] E2E тесты Playwright

### Задача

Реализовать E2E-тесты для критических путей (project.md 11.3).

### Решение

- Установлен @playwright/test, создан playwright.config.ts
- tests/e2e/guest.spec.ts: главная, /login, /register, /privacy
- tests/e2e/patient-flow.spec.ts: dev-login → дашборд → лекарства → добавление → подтверждение приёма
- CI: добавлен job e2e с postgres/redis, migrate, seed, playwright test
- Скрипты: npm run test:e2e, npm run test:e2e:ui

---

## [2026-02-22] README для проекта

### Задача

Заменить шаблонный README Next.js на актуальную документацию MemoMed AI.

### Решение

Обновлён README.md:

- Описание проекта и возможностей
- Требования (Node.js, Docker, DEEPSEEK_API_KEY)
- Быстрый старт (setup.sh, npm run dev)
- Таблица скриптов
- Тестовые аккаунты после seed
- Ссылки на docs/

---

## [2026-02-22] CONTRIBUTING.md

### Задача

Добавить руководство для контрибьюторов (Git-процесс, стандарты, проверки).

### Решение

Создан CONTRIBUTING.md:

- Ссылки на project.md, tasktracker.md, diary.md
- Настройка окружения
- Git: ветки, Conventional Commits, PR, pre-commit
- Команды перед коммитом (lint, type-check, test)
- E2E локально
- Стандарты кода
- Обновление документации после изменений

---

## [2026-02-22] Аудит кода и дизайна

### Задача

Провести аудит всего кода и дизайна проекта.

### Результат

Создан docs/audit-2026-02-22.md:

**Код**: TypeScript без any, валидация Zod, auth на API, rate limiting — в порядке. Рекомендации: логирование в пустых catch, placeholder в connect-form.

**Дизайн**: Design system, touch 48px, focus styles — ок. Замечания: text-xs/text-sm ниже 18px для 60+, min-h 40–44px в части компонентов. Рекомендации: заменить text-xs на text-sm, увеличить min-h до 48px.

### Внедрение рекомендаций аудита (2026-02-22)

Выполнены все приоритеты из audit-2026-02-22.md:

- **Высокий**: text-xs → text-sm в patient, relative, doctor страницах и компонентах (header, feed-item, stats-cards, calendar-view, analysis-results, journal-form, feedback-form, photo-upload, ai-chat, message-bubble, tts-settings, discipline-chart, live-feed)
- **Средний**: min-h 40/44px → 48px в chat-suggestions, sync-status, live-feed
- **Низкий**: логирование в catch для medications, log-actions, logs, storage; journal-form getSliderColor — хардкод заменён на var(--color-success), var(--color-error), var(--color-warning)

---

## [2026-02-22] Integration-тест API /api/locale

### Задача

Добавить integration-тест для API route (project.md 11.3: Integration-тесты для API routes).

### Решение

Создан tests/integration/api-locale.test.ts:

- Вызов POST handler напрямую с Request
- Валидные локали ru, en → 200, set-cookie
- Невалидная локаль de → 400
- Пустое тело → 400
