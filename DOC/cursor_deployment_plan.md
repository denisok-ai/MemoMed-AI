# 🚀 План развертывания Smart Med Assistant в Cursor.com

## 📋 АНАЛИЗ ПРОЕКТА

### Что было разработано в чате:
1. ✅ **SQL-схема PostgreSQL** - полная структура базы данных
2. ✅ **Backend API (Node.js)** - Express сервер с аутентификацией
3. ✅ **Flutter архитектура** - структура проекта и зависимости
4. ✅ **Техническая документация** - детальное ТЗ
5. ✅ **Deployment скрипты** - автоматизация установки на AlmaLinux

### Технологический стек:
- **Frontend:** Flutter (Dart)
- **Backend:** Node.js + Express
- **База данных:** PostgreSQL 14
- **Аутентификация:** JWT
- **Уведомления:** Firebase Cloud Messaging
- **Локальное хранилище:** SQLite (offline-first)

---

## 🎯 ПОШАГОВЫЙ ПЛАН ДЛЯ CURSOR

### ЭТАП 1: Подготовка окружения (10 мин)

#### Шаг 1.1: Установка Cursor
1. Скачай Cursor с https://cursor.com/
2. Установи на свой компьютер (Windows/Mac/Linux)
3. Открой Cursor и авторизуйся

#### Шаг 1.2: Установка необходимого софта
**На Windows:**
```powershell
# Установи Node.js 18+
winget install OpenJS.NodeJS.LTS

# Установи PostgreSQL
winget install PostgreSQL.PostgreSQL

# Установи Git
winget install Git.Git
```

**На Mac:**
```bash
# Через Homebrew
brew install node
brew install postgresql@14
brew install git
```

**На Linux:**
```bash
sudo apt update
sudo apt install nodejs npm postgresql-14 git -y
```

---

### ЭТАП 2: Создание проекта в Cursor (15 мин)

#### Шаг 2.1: Создай новую папку проекта
```bash
mkdir smart-med-assistant
cd smart-med-assistant
```

#### Шаг 2.2: Открой папку в Cursor
1. Открой Cursor
2. File → Open Folder
3. Выбери папку `smart-med-assistant`

#### Шаг 2.3: Используй AI Chat в Cursor
Нажми `Cmd+L` (Mac) или `Ctrl+L` (Windows) чтобы открыть Cursor Chat

**Промпт для Cursor AI:**
```
Создай структуру проекта для медицинского приложения:

1. Backend (Node.js + Express + PostgreSQL)
2. Frontend (Flutter)

Создай следующие папки и файлы:
- backend/
  - server.js
  - package.json
  - .env.example
  - routes/
  - models/
  - middleware/
- frontend/
  - lib/
  - pubspec.yaml
- database/
  - schema.sql
- docs/
  - README.md
  - API.md
```

---

### ЭТАП 3: Настройка Backend (20 мин)

#### Шаг 3.1: Скопируй файлы из чата в Cursor

**1. Создай `backend/package.json`**

В Cursor Chat напиши:
```
Создай package.json для Express сервера с зависимостями:
- express
- pg (PostgreSQL)
- dotenv
- cors
- jsonwebtoken
- bcrypt
- node-cron

Добавь скрипты:
- start: запуск сервера
- dev: запуск с nodemon
```

**2. Создай `database/schema.sql`**

Скопируй из артефакта "PostgreSQL Schema - Smart Med Assistant" (из нашего чата выше)

**3. Создай `backend/server.js`**

Скопируй из артефакта "server.js - Backend API"

**4. Создай `backend/.env`**

```env
NODE_ENV=development
PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smart_med_assistant
DB_USER=med_admin
DB_PASSWORD=твой_пароль_здесь
JWT_SECRET=твой_jwt_секрет_здесь
```

#### Шаг 3.2: Установка зависимостей

В терминале Cursor (Terminal → New Terminal):
```bash
cd backend
npm install
```

---

### ЭТАП 4: Настройка PostgreSQL (15 мин)

#### Шаг 4.1: Запуск PostgreSQL

**Windows:**
```powershell
# Запусти службу PostgreSQL
net start postgresql-x64-14
```

**Mac:**
```bash
brew services start postgresql@14
```

**Linux:**
```bash
sudo systemctl start postgresql
```

#### Шаг 4.2: Создание базы данных

**Промпт для Cursor AI:**
```
Создай bash-скрипт setup_db.sh который:
1. Создает базу данных smart_med_assistant
2. Создает пользователя med_admin с паролем
3. Импортирует schema.sql
4. Назначает права пользователю

Для PostgreSQL на localhost.
```

Затем выполни:
```bash
chmod +x setup_db.sh
./setup_db.sh
```

#### Альтернатива (ручная настройка):
```bash
# Подключись к PostgreSQL
psql -U postgres

# В консоли PostgreSQL:
CREATE DATABASE smart_med_assistant;
CREATE USER med_admin WITH PASSWORD 'твой_пароль';
GRANT ALL PRIVILEGES ON DATABASE smart_med_assistant TO med_admin;
\q

# Импорт схемы
psql -U med_admin -d smart_med_assistant -f database/schema.sql
```

---

### ЭТАП 5: Запуск Backend (5 мин)

#### Шаг 5.1: Тестовый запуск

В терминале Cursor:
```bash
cd backend
npm start
```

Должен увидеть:
```
✅ Server running on http://localhost:3000
✅ PostgreSQL подключен успешно
```

#### Шаг 5.2: Проверка API

В новом терминале:
```bash
curl http://localhost:3000/health
```

Должен вернуть JSON с статусом OK.

---

### ЭТАП 6: Настройка Flutter Frontend (30 мин)

#### Шаг 6.1: Установка Flutter

**Промпт для Cursor AI:**
```
Напиши инструкцию по установке Flutter SDK для моей ОС.
Включи настройку PATH и проверку через flutter doctor.
```

#### Шаг 6.2: Создание проекта Flutter

```bash
cd frontend
flutter create .
```

#### Шаг 6.3: Настройка pubspec.yaml

**Промпт для Cursor AI:**
```
Обнови pubspec.yaml для медицинского приложения:

Добавь зависимости:
- provider (state management)
- http (API запросы)
- sqflite (локальная БД)
- flutter_local_notifications (уведомления)
- shared_preferences (настройки)
- intl (форматирование дат)
- table_calendar (календарь)

Используй минималистичный дизайн с шрифтом без засечек.
```

#### Шаг 6.4: Установка зависимостей

```bash
flutter pub get
```

---

### ЭТАП 7: Создание базовых экранов Flutter (40 мин)

#### Шаг 7.1: Структура проекта

**Промпт для Cursor AI:**
```
Создай структуру папок для Flutter приложения:

lib/
├── main.dart
├── core/
│   ├── constants/
│   │   ├── colors.dart (воздушный дизайн: белый, желтый, зеленый)
│   │   └── text_styles.dart (крупные адаптивные размеры)
│   └── utils/
├── data/
│   ├── models/
│   │   ├── user.dart
│   │   ├── medication.dart
│   │   └── medication_log.dart
│   ├── local/ (SQLite)
│   └── remote/ (API)
├── domain/
│   └── services/
│       ├── sync_service.dart
│       └── notification_service.dart
└── presentation/
    ├── screens/
    │   ├── patient/
    │   │   ├── home_screen.dart (главный экран с часами)
    │   │   └── medication_list.dart
    │   └── relative/
    │       ├── live_feed.dart
    │       └── calendar_view.dart
    └── widgets/
        ├── big_green_button.dart (кнопка подтверждения)
        └── clock_widget.dart (адаптивные часы 15% ширины)
```

#### Шаг 7.2: Создание главного экрана пациента

**Промпт для Cursor AI:**
```
Создай home_screen.dart для пациента с:

1. Динамическим фоном (белый по умолчанию, желтый при напоминании)
2. Адаптивными часами (15% ширины экрана)
3. Большой круглой зеленой кнопкой внизу с галочкой
4. Анимацией при нажатии (двойная мягкая вибрация)
5. Минималистичным дизайном без лишних элементов

Используй Provider для state management.
Экран должен подходить для пожилых людей.
```

#### Шаг 7.3: Создание модели данных

**Промпт для Cursor AI:**
```
Создай Dart модели для:

1. User (id, email, role, fullName)
2. Medication (id, name, dosage, scheduledTime, instruction)
3. MedicationLog (id, medId, scheduledAt, actualAt, status)

Добавь методы:
- toJson() / fromJson()
- toMap() / fromMap() для SQLite

Используй UUID для id.
```

---

### ЭТАП 8: Интеграция Backend с Frontend (25 мин)

#### Шаг 8.1: Создание API Service

**Промпт для Cursor AI:**
```
Создай api_service.dart с методами:

1. login(email, password) → JWT токен
2. getMedications(patientId) → List<Medication>
3. syncLogs(logs) → синхронизация offline данных
4. getRelativeFeed(relativeId) → живая лента событий

Используй http package.
Обрабатывай ошибки (try-catch).
Храни JWT в secure_storage.
```

#### Шаг 8.2: Настройка локальной БД (SQLite)

**Промпт для Cursor AI:**
```
Создай database_helper.dart для SQLite:

1. Таблицы: medications, logs (offline буфер)
2. Методы CRUD для medications
3. Метод getUnsyncedLogs() для синхронизации
4. Версионирование базы данных

Используй sqflite package.
```

---

### ЭТАП 9: Система уведомлений (20 мин)

#### Шаг 9.1: Настройка локальных уведомлений

**Промпт для Cursor AI:**
```
Создай notification_service.dart с:

1. Настойчивой системой напоминаний:
   - T+0: уведомление + легкий звук
   - T+10: повтор + усиленная вибрация
   - T+20: максимальная громкость
   - T+30: экстренное уведомление родственнику

2. Метод scheduleNotification(medication)
3. Метод cancelNotification(id)

Используй flutter_local_notifications.
```

#### Шаг 9.2: Интеграция Firebase (опционально)

**Промпт для Cursor AI:**
```
Настрой Firebase Cloud Messaging для:
1. Push-уведомлений родственникам
2. Регистрации device token
3. Обработки фоновых уведомлений

Создай firebase_messaging_service.dart
```

---

### ЭТАП 10: Тестирование и отладка (30 мин)

#### Шаг 10.1: Запуск приложения

```bash
# В одном терминале (Backend)
cd backend
npm start

# В другом терминале (Flutter)
cd frontend
flutter run
```

#### Шаг 10.2: Тестовые сценарии

**Промпт для Cursor AI:**
```
Создай integration тесты для:

1. Регистрация пользователя
2. Добавление лекарства
3. Подтверждение приема
4. Offline-online синхронизация
5. Система напоминаний

Используй flutter_test.
```

#### Шаг 10.3: Отладка с Cursor AI

При ошибках используй Cursor Chat:
```
У меня ошибка: [скопируй текст ошибки]

Контекст: [опиши что пытался сделать]

Помоги исправить.
```

---

### ЭТАП 11: Оптимизация и улучшения (опционально)

#### Промпты для Cursor AI:

**Производительность:**
```
Оптимизируй производительность:
1. Lazy loading для списков
2. Кеширование API запросов
3. Сжатие изображений
4. Debounce для поиска
```

**Безопасность:**
```
Улучши безопасность:
1. Валидация всех входных данных
2. Rate limiting для API
3. XSS/SQL injection защита
4. HTTPS обязательно для продакшна
```

**UI/UX:**
```
Улучши интерфейс:
1. Скелетон-загрузка
2. Плавные анимации
3. Темная тема (опционально)
4. Адаптивность под планшеты
```

---

## 📦 ПОЛНЫЙ КОМПЛЕКТ ФАЙЛОВ ДЛЯ CURSOR

### Файлы, которые нужно создать:

```
smart-med-assistant/
├── README.md (общее описание проекта)
├── backend/
│   ├── package.json
│   ├── server.js
│   ├── .env
│   ├── .env.example
│   └── routes/
│       ├── auth.js
│       ├── medications.js
│       └── logs.js
├── frontend/
│   ├── pubspec.yaml
│   ├── lib/
│   │   ├── main.dart
│   │   ├── core/
│   │   ├── data/
│   │   ├── domain/
│   │   └── presentation/
│   └── android/ (Flutter auto-generated)
├── database/
│   ├── schema.sql
│   └── migrations/
└── docs/
    ├── API.md (документация API)
    ├── SETUP.md (инструкция по установке)
    └── ARCHITECTURE.md (архитектура проекта)
```

---

## 🎯 БЫСТРЫЙ СТАРТ В CURSOR

### Вариант 1: Пошаговый (рекомендуется для обучения)
Следуй этапам 1-11 последовательно, используя Cursor AI для генерации кода.

### Вариант 2: Автоматический (для опытных)

**Мега-промпт для Cursor AI:**
```
Создай полный проект Smart Med Assistant:

BACKEND (Node.js + Express + PostgreSQL):
- Аутентификация через JWT
- CRUD для medications
- Синхронизация offline логов
- Живая лента для родственников

FRONTEND (Flutter):
- Главный экран с адаптивными часами
- Большая зеленая кнопка подтверждения
- Offline-first с SQLite
- Настойчивые напоминания (T+0, T+10, T+20, T+30)
- Минималистичный дизайн для пожилых

DATABASE:
- PostgreSQL схема из файла schema.sql
- Таблицы: users, medications, logs, connections

Используй:
- Flutter provider для state
- http для API
- sqflite для offline
- flutter_local_notifications

Создай полную структуру проекта с README.
```

---

## 🆘 ПОМОЩЬ И ПОДДЕРЖКА

### Если что-то не работает:

**В Cursor Chat спрашивай:**
```
@codebase Почему [компонент] не работает?

@workspace Покажи все файлы связанные с [фичей]

@file:server.js Объясни эту функцию

@docs Как настроить [технологию]?
```

### Полезные команды Cursor:

- `Cmd+K` (Mac) / `Ctrl+K` (Win) - редактирование кода с AI
- `Cmd+L` (Mac) / `Ctrl+L` (Win) - чат с AI
- `Cmd+Shift+P` - палитра команд
- `Cmd+.` - показать фиксы для ошибок

---

## ✅ ЧЕКЛИСТ ГОТОВНОСТИ

Перед запуском проверь:

- [ ] Node.js установлен (node --version)
- [ ] PostgreSQL запущен
- [ ] Flutter SDK настроен (flutter doctor)
- [ ] Все зависимости установлены (npm install, flutter pub get)
- [ ] База данных создана и схема импортирована
- [ ] .env файл настроен с правильными паролями
- [ ] Backend отвечает на /health
- [ ] Flutter приложение компилируется без ошибок

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ ПОСЛЕ MVP

1. **Деплой Backend** - на Heroku, Railway, или VPS
2. **Публикация в Store** - Google Play / App Store
3. **SSL сертификат** - для HTTPS
4. **Домен** - для профессионального вида
5. **Мониторинг** - Sentry для отслеживания ошибок
6. **Аналитика** - Firebase Analytics для метрик

---

**Время на полную установку: ~2-3 часа**

**Время на базовый MVP: ~1 неделя активной разработки**
