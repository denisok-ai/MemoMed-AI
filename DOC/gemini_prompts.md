# 🤖 Промпты для Gemini CLI

## Инструкция по использованию:
1. Копируй промпт целиком
2. Вставляй в Gemini CLI
3. Выполняй сгенерированные команды
4. Пиши результат следующим промптом
5. Gemini даст следующий шаг

---

## 📍 ПРОМПТ 1: Диагностика текущего состояния

```
Ты - DevOps эксперт. Я настраиваю медицинское приложение на AlmaLinux 8 сервере (IP: 194.87.0.45).

Текущая ситуация:
- PostgreSQL 14 установлен
- Node.js 18 установлен  
- База данных smart_med_assistant создана
- Пользователь med_admin создан
- Проблема: ошибка аутентификации при подключении к PostgreSQL

Создай bash-скрипт для:
1. Проверки статуса PostgreSQL
2. Проверки существующих баз данных
3. Проверки файла pg_hba.conf
4. Вывода текущих настроек аутентификации

Скрипт должен:
- Выводить понятные сообщения на русском
- Показывать прогресс выполнения
- Сохранять результаты в файл /root/diagnostic_report.txt
- Быть безопасным для выполнения на продакшн сервере

Начни с создания скрипта check_system.sh
```

---

## 📍 ПРОМПТ 2: Исправление PostgreSQL (после диагностики)

```
Спасибо за диагностику! Результаты:
[ВСТАВЬ СЮДА ВЫВОД ИЗ diagnostic_report.txt]

Теперь создай bash-скрипт fix_postgresql.sh который:

1. Создаст резервную копию /var/lib/pgsql/14/data/pg_hba.conf
2. Исправит настройки аутентификации для:
   - База: smart_med_assistant
   - Пользователь: med_admin
   - Метод: md5 для local и host подключений
3. Перезапустит PostgreSQL безопасно
4. Проверит успешность перезапуска
5. Выведет итоговый отчет на русском

Сохрани мой DB_PASSWORD в переменную окружения для использования:
DB_PASSWORD="[ВСТАВЬ СВОЙ ПАРОЛЬ СЮДА]"

Скрипт должен показывать прогресс и останавливаться при ошибках.
```

---

## 📍 ПРОМПТ 3: Импорт SQL-схемы

```
PostgreSQL настроен! Теперь нужно создать таблицы в базе данных.

Создай скрипт import_schema.sh который:

1. Создаст файл schema.sql с такими таблицами:
   - users (id, email, password_hash, role, invite_code)
   - profiles (user_id, full_name, timezone, region_code)
   - medications (patient_id, name, dosage, scheduled_time, is_active)
   - medication_logs (med_id, scheduled_at, actual_at, status)
   - connections (patient_id, relative_id, status)

2. Импортирует схему через: sudo -u postgres psql

3. Назначит права пользователю med_admin на все таблицы

4. Проверит успешность создания командой \dt

5. Выведет количество созданных таблиц на русском

Учти:
- Используй UUID для id
- Добавь created_at для всех таблиц
- Создай индексы для medications и logs
- База данных: smart_med_assistant

Прогресс показывай эмодзи: 🔄 процесс, ✅ готово, ❌ ошибка
```

---

## 📍 ПРОМПТ 4: Создание Backend API

```
База данных готова! Теперь создаем Node.js сервер.

Создай три файла в директории ~/smart-med-backend:

1. server.js - Express.js сервер с:
   - Подключением к PostgreSQL (pg библиотека)
   - CORS для всех источников
   - Endpoint GET /health (проверка БД + время)
   - Endpoint GET /api/test (тестовый)
   - Вывод в консоль статуса запуска
   - Порт 3000, слушать на 0.0.0.0

2. .env - конфигурация:
   - DB_HOST=localhost
   - DB_PORT=5432
   - DB_NAME=smart_med_assistant
   - DB_USER=med_admin
   - DB_PASSWORD=[ПОПРОСИТ ВВЕСТИ]
   - PORT=3000
   - SERVER_IP=194.87.0.45

3. startup.sh - скрипт запуска:
   - Проверка установки зависимостей (npm install если нужно)
   - Тест подключения к БД
   - Запуск сервера через npm start
   - Вывод URL для проверки

Все файлы создавай с комментариями на русском.
Сервер должен логировать все запросы с временем.
```

---

## 📍 ПРОМПТ 5: Тестирование и запуск

```
Backend создан! Теперь запускаем и тестируем.

Создай скрипт test_backend.sh который:

1. Запустит сервер в фоне (через nohup или screen)
2. Подождет 5 секунд
3. Выполнит тестовые запросы:
   - curl http://194.87.0.45:3000/health
   - curl http://194.87.0.45:3000/api/test
4. Проверит, что сервер отвечает HTTP 200
5. Выведет красивый отчет:
   - ✅ Сервер запущен
   - ✅ База данных подключена
   - ✅ API работает
   - 📊 Количество таблиц: X
   - 🌐 URL: http://194.87.0.45:3000

Если ошибки - покажет логи последних 20 строк.

Также создай команду для остановки сервера (stop_backend.sh)
```

---

## 📍 ПРОМПТ 6: Настройка firewall

```
Всё работает локально! Теперь открываем порты.

Создай скрипт setup_firewall.sh который:

1. Проверит, запущен ли firewalld
2. Откроет порты:
   - 3000/tcp (Backend API)
   - 5432/tcp (PostgreSQL - опционально)
3. Сохранит правила permanently
4. Перезагрузит firewall
5. Выведет список открытых портов

Также добавь проверку извне:
- curl http://194.87.0.45:3000/health

Если curl работает - значит порт открыт.
Результат выведи на русском с эмодзи.
```

---

## 📍 ПРОМПТ 7: Создание systemd сервиса (автозапуск)

```
Последний шаг - автозапуск при перезагрузке сервера.

Создай:

1. Файл /etc/systemd/system/smart-med-backend.service
   - Описание на русском
   - User=root
   - WorkingDirectory=/root/smart-med-backend
   - ExecStart=/usr/bin/node server.js
   - Restart=always
   - Environment переменные из .env

2. Скрипт setup_autostart.sh который:
   - Создаст service файл
   - Выполнит systemctl daemon-reload
   - Включит автозапуск: systemctl enable
   - Запустит сервис: systemctl start
   - Проверит статус: systemctl status
   - Выведит итог на русском

3. Команды управления:
   - sudo systemctl status smart-med-backend
   - sudo systemctl restart smart-med-backend
   - sudo systemctl stop smart-med-backend
   - sudo journalctl -u smart-med-backend -f (логи)

Выведи инструкцию на русском как пользоваться.
```

---

## 🎯 КАК ИСПОЛЬЗОВАТЬ:

### Шаг 1: Вставь ПРОМПТ 1 в Gemini CLI
```bash
gemini "ТЕКСТ_ПРОМПТА_1"
```

### Шаг 2: Gemini сгенерирует скрипт - выполни его
```bash
bash check_system.sh
```

### Шаг 3: Скопируй результат и вставь в ПРОМПТ 2
```bash
cat /root/diagnostic_report.txt
# Скопируй вывод и вставь в промпт 2
```

### Шаг 4: Повторяй для всех промптов по порядку

---

## ⚡ БЫСТРЫЙ СТАРТ (всё за раз):

Если хочешь выполнить всё автоматом, используй этот мега-промпт:

```
Ты - эксперт DevOps. Создай ОДИН master-скрипт deploy_all.sh для AlmaLinux 8 который выполнит ВСЁ последовательно:

1. Диагностика системы
2. Исправление PostgreSQL
3. Импорт SQL-схемы
4. Создание Backend API
5. Тестирование
6. Настройка firewall
7. Настройка автозапуска

Скрипт должен:
- Показывать прогресс каждого этапа (1/7, 2/7...)
- Спрашивать подтверждение перед каждым этапом
- Логировать всё в /root/deploy.log
- Останавливаться при ошибках
- Выводить итоговый отчет на русском

Параметры:
- IP: 194.87.0.45
- БД: smart_med_assistant
- Юзер: med_admin
- Пароль: спросить в начале скрипта

Начни с создания deploy_all.sh
```

---

**Какой вариант выбираешь?**
- A) Пошагово (ПРОМПТ 1 → 7)
- B) Всё сразу (мега-промпт)
